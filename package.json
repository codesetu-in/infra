{
  "name": "@deploycloud/infra",
  "version": "0.0.1",
  "private": true,
  "description": "DeployCloud Terraform AWS infrastructure",
  "scripts": {
    "fmt": "terraform fmt -recursive .",
    "lint": "terraform fmt -check -recursive .",
    "prepare": "husky"
  },
  "engines": {
    "node": ">=22",
    "pnpm": ">=10"
  },
  "packageManager": "pnpm@10.0.0",
  "devDependencies": {
    "@commitlint/cli": "^19.0.0",
    "@commitlint/config-conventional": "^19.0.0",
    "husky": "^9.0.0",
    "lint-staged": "^15.0.0"
  }
}
